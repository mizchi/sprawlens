node_modules/
dist/
.codesprawl/
.DS_Store
*.log
.playwright-mcp/
codesprawl-*.png
test-results/
perf-results/

# dotenvx: keep secrets local-only on this public repo (don't commit even
# the encrypted .env). dotenvx run -- decrypts via .env.keys at runtime.
.env
.env.keys

# captured self-trace artifacts (machine-specific timings/paths; regenerate
# with the capture script)
e2e/__trace__/
